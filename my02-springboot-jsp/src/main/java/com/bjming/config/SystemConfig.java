package com.bjming.config;

import org.springframework.context.annotation.Configuration;

/**
 * @Configuration: 声明这是一个自动配置类, 相当于一个xml配置文件,
 * 只有@Configuration注解声明的类, 内部的@Bean注解才会生效
 */
@Configuration
public class SystemConfig {

}
